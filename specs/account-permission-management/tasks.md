# 实施清单

- [x] 1. 建立共享账号权限核心
  - 在 `cicada-order-workflow` 中定义完整权限目录、中文分组、角色默认模板和权限依赖。
  - 实现账号最终权限计算、非法权限过滤、管理员全权和旧账号模板回退。
  - 保留旧 RBAC 导出作为兼容层，并同步 `cicada-admin-order` 的工作流 fallback。
  - 增加角色模板、自定义覆盖、空权限、非法键和管理员全权测试。
  - _需求：2、3、4、5、6、8、9、10、11_

- [x] 2. 扩展员工数据 Schema
  - 为 `cicada_users` 增加 `permissions` 和 `permission_version` 字段定义。
  - 确认不需要新增集合或索引，并保持小程序 `role=user` 记录兼容。
  - 增加 Schema 契约测试，验证空权限数组和现有角色枚举可用。
  - _需求：2、3、9、10_

- [x] 3. 改造员工登录与账号管理后端
  - 让管理员登录和权限刷新接口返回安全的最终权限清单。
  - 为用户管理页提供权限目录与角色模板接口。
  - 扩展 `manageStaff`，支持新增/编辑账号权限、非法键校验、越权授予拦截和超管保护。
  - 保持自我禁用、临时密码、会话清理和敏感字段过滤规则。
  - 在审计日志记录权限增删变化，不记录密码或 Token。
  - 增加新增多客服、不同权限、越权授予、自我禁用和敏感字段测试。
  - 将员工查看、新增、编辑、启停和密码重置拆为独立权限。
  - _需求：1、2、3、4、8、10、11_

- [x] 4. 拆分工单、维修与物流权限
  - 将备注、维修记录、收件明细和工单产品从 `update_remarks` 拆为独立权限。
  - 将任意状态修改、签收入库、收件配件确认和恢复取消拆为独立权限。
  - 将单笔回寄录入与批量签收/回寄导入拆为独立权限。
  - 让 `getWorkflowConfig` 按账号最终权限返回配置，并移除页面所依赖的角色硬判断。
  - 增加维修师傅、客服1、只读客服和财务的关键接口允许/拒绝测试。
  - _需求：4、5、7、10、12、13_

- [x] 5. 拆分配件与库存权限
  - 将配件查看、编辑、采购入库、手工出库、盘点调整、导入和导出从 `manage_inventory` 拆开。
  - 维修记录选择配件只要求查看权限，不开放成本价、库存修改或导出。
  - 保持库存流水、扣减、异常恢复和成本数据的敏感权限边界。
  - 增加维修师傅只读配件和管理员单独授予入库权限的测试。
  - _需求：2、5、6、8_

- [x] 6. 统一客户、知识库、反馈与系统接口鉴权
  - 将客户查看、完整手机号、编辑、设备、导入、导出和注销改为共享账号权限判断。
  - 让客服可查看脱敏客户资料但不能导出或查看完整手机号。
  - 将知识库及 `cicada-admin-sys` 内剩余固定角色判断迁移为权限键。
  - 验证客服反馈处理、财务审计和管理员系统设置权限不串权。
  - _需求：4、7、12、13_

- [x] 7. 建立 PC 端统一权限会话与路由门禁
  - 新增统一的 `hasPermission`、权限刷新和权限变化处理逻辑。
  - 将 `menuAccess.js`、路由守卫和侧边菜单从固定角色改为权限键。
  - 登录、刷新和个人资料更新时保留正确权限，退出时清除权限会话。
  - 后端返回无权限时提示权限已变化并刷新访问配置。
  - 增加菜单映射和会话权限工具测试。
  - _需求：3、4、7、10、13_

- [x] 8. 完成员工账号与权限配置界面
  - 在新增/编辑员工弹窗中加入角色模板、分组复选框、权限数量摘要和组内全选/清空。
  - 实现管理员、维修师傅、客服、财务模板填充及账号级调整。
  - 为账号管理、客户导出、付款/退款和系统设置等高风险权限增加明确提示。
  - 列表展示权限摘要，并适配桌面与窄屏布局。
  - 验证新增多个客服账号且保存不同权限清单。
  - _需求：1、2、3、8、11_

- [x] 9. 改造业务页面按钮和区块门禁
  - 工单页分别控制签收、收件明细、维修记录、回寄、报价、状态和导出入口。
  - 配件页分别控制查看、编辑、调整、导入和导出入口。
  - 客户、财务、结算、发票、物流、反馈、审计和设置页面使用对应权限。
  - 清除 `role === ...` 一类会绕过账号自定义权限的业务判断。
  - _需求：4、5、6、7、8、12、13_

- [x] 10. 完成本地验收与安全复核
  - 运行共享模块及各云对象相关 Node 测试，修复所有回归。
  - 运行 `docte-master/npm run check`。
  - 运行 `pc-admin/npm run build` 及相关定向检查脚本。
  - 审查登录/账号接口响应，确认无密码、哈希、Token 或会话泄漏。
  - 核对现有管理员、紧急管理员、客服、维修工程师和财务的模板行为。
  - 输出部署文件清单和线上最小验收步骤；不在本任务内自动部署或修改真实账号。
  - _需求：全部_

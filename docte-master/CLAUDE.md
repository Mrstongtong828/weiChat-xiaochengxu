# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

> **Not authoritative.** The canonical project guide lives at the repository root:
> [`../CLAUDE.md`](../CLAUDE.md). Read that one — it has the correct repo layout,
> build/run commands, backend architecture, RBAC model, and conventions.
>
> This directory (`docte-master/`) is the active mini-program **and** the shared
> `uniCloud-alipay/` backend, but everything you need to know about it is documented
> in the root guide.

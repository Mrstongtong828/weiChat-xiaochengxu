# 上线配置清单（逐条照做）

> 正式服务空间：支付宝云 **cicada-aftersales**（`env-00jy6g4qwi94`）。云函数代码已部署，本清单是**控制台/后台侧**还要做的配置。
> 约定：环境变量名区分大小写，按下表原样填。配好后**重新部署一次对应云函数或等几分钟生效**。

> **2026-07-20 上线状态：** 小程序构建、PC Admin 构建、订阅配置、后台 URL、安全和错误处理检查均已通过。当前主要剩余真实管理员登录、角色权限、小程序真机业务冒烟测试，以及小程序上传审核发布。

## 快递100物流

先在快递100企业版开通“实时查询”和“订阅推送”，取得 `KEY` 与 `CUSTOMER`。在 uniCloud 服务空间配置以下全局环境变量：

| 环境变量名 | 值 / 来源 | 必填 |
|---|---|---|
| `EXPRESS_PROVIDER` | 固定填 `kuaidi100` | ✅ |
| `KUAIDI100_KEY` | 快递100企业版授权 Key | ✅ |
| `KUAIDI100_CUSTOMER` | 快递100企业版 customer 授权码 | ✅ |
| `KUAIDI100_CALLBACK_URL` | `cicada-express-callback/notify` 的公网 URL 化地址 | ✅（订阅推送） |
| `KUAIDI100_CALLBACK_SALT` | 自行生成的至少 32 位随机字符串，用于回调验签 | ✅（订阅推送） |

部署 `cicada-client-order`、`cicada-admin-order`、`cicada-express-callback`，并给新回调云函数开启 URL 化。密钥未配置时物流查询自动回退为工单状态估算，不会阻断报修、导入和发货。

> 顺丰、顺丰快运、中通等查询需要收/寄件完整手机号。录入物流时应确保订单对应的寄件或收件信息中有手机号，否则快递100可能返回手机号校验失败。

---

## A. 微信支付（不配则下单付款报“微信支付暂未配置”）

读取位置：`cicada-client-order` 的 `getWechatPayConfig()` / `getWechatPayApiV3Key()`。

设置位置：uniCloud Web 控制台 https://unicloud.dcloud.net.cn → 选服务空间 **doctor-app** → 云函数 → **环境变量 / 公共配置**（让 `cicada-client-order` 能读到 `process.env`）。

| 环境变量名 | 值 / 来源 | 必填 |
|---|---|---|
| `WX_PAY_APPID` | 小程序 AppID `wx25289fbe4a3bf011`（也可复用 `WX_APPID`） | ✅ |
| `WX_PAY_MCH_ID` | 微信支付商户号（商户平台 → 账户中心 → 商户号） | ✅ |
| `WX_PAY_SERIAL_NO` | 商户 API 证书序列号（商户平台 → API 安全 → API 证书） | ✅ |
| `WX_PAY_PRIVATE_KEY` | 商户 API 私钥 `apiclient_key.pem` 全文（含 `-----BEGIN/END-----`，换行用 `\n`）<br>或 `WX_PAY_PRIVATE_KEY_BASE64` 填该 pem 的 base64 | ✅（二选一） |
| `WX_PAY_API_V3_KEY` | APIv3 密钥（商户平台 → API 安全 → APIv3 密钥，32 位） | ✅（支付回调解密用） |
| `WX_PAY_NOTIFY_URL` | 支付结果回调 URL，见下方 A-1 | ✅ |

### A-1. 回调 URL（WX_PAY_NOTIFY_URL）
1. uniCloud 控制台为 `cicada-client-order` 开启 **云函数 URL 化**，得到形如 `https://xxx.bspapp.com/http/cicada-client-order` 的地址。
2. `WX_PAY_NOTIFY_URL` 填指向 `wechatPayNotify` 方法的 URL 化地址（该方法已写好，靠 `this.getHttpInfo()` 解析微信回调并解密 → 以服务端查单结果为准）。
3. 在**微信支付商户平台**把该域名加入 Native/JSAPI 支付回调域名白名单。

> 也支持 `WXPAY_*` / `WECHAT_PAY_*` 前缀（同名取其一即可），但建议统一用 `WX_PAY_*`。

---

## B. 微信订阅消息（不配则提醒静默跳过，写一条 skipped 日志，不报错）

### B-1. 公众号/小程序基础（发订阅消息要先拿 access_token）
读取位置：`getWechatAppConfig()`。

| 环境变量名 | 值 |
|---|---|
| `WX_APPID` | `wx25289fbe4a3bf011` |
| `WX_SECRET` | 小程序密钥（微信公众平台 → 开发管理 → 开发设置 → AppSecret） |

### B-2. 5 个订阅模板 ID
在**微信公众平台 → 订阅消息**保留现有 5 套模板，拿到模板 ID 后按下表填环境变量（命名规则 `WX_SUBSCRIBE_TEMPLATE_<模板键>`）：

| 环境变量名 | 触发场景 |
|---|---|
| `WX_SUBSCRIBE_TEMPLATE_REPAIR_SUBMIT` | 报修受理通知 |
| `WX_SUBSCRIBE_TEMPLATE_DEVICE_RECEIVE_SHIP` | 设备签收、回寄发货共用 |
| `WX_SUBSCRIBE_TEMPLATE_PAYMENT_QUOTE` | 维修报价、付款成功/驳回共用 |
| `WX_SUBSCRIBE_TEMPLATE_PROCESS_TIP` | 报修进度提醒（按需保留） |
| `WX_SUBSCRIBE_TEMPLATE_ORDER_FINISH_INVOICE` | 工单完成，并在维修结果中附发票提示 |

> 模板 ID 配在哪个云函数：`cicada-client-order`（报修/支付/完成）与 `cicada-admin-order`（签收/报价/支付审核/回寄/完成）都要能读到，建议设为**服务空间全局环境变量**。报价不再单独配置，开票不再单独推送。
>
> `inspection_completed`、`repair_started`、`review_invite` 已下线；旧的 7 个场景变量和计划新增的报价/发票变量不再读取。
>
> 5 套模板的完整 ID 和关键词已于 2026-07-20 通过微信官方接口核对，代码按模板分别构造字段，不再假定模板共用关键词。逐字段定义与可直接联调的标准文案见 [`订阅消息填参规范.md`](订阅消息填参规范.md)。

---

## C. 数据库索引（上线前必须核对类型）

设置位置：uniCloud Web 控制台 → 数据库 → 选集合 → **索引管理**。完整清单见 `uniCloud-alipay/database/INDEXES.md`。

当前支付宝云环境必须以控制台显示的字段类型为准。HBuilderX CLI 初始化 `.index.json` 时，`long/int/bool/double/array` 字段曾被落成为 `varchar`，因此非字符串索引必须在控制台删除后手动重建并复核类型。

必须确认：

- `cicada_orders.order_no` —— **唯一索引**（当前 `uniq_order_no` 已完成，勿删除）
- `cicada_orders` 复合：`customer_id, create_time desc`（身份桥后台查历史工单）
- `cicada_customers`：`user_id`、`openid`、`phone`（身份桥下单匹配/建档）
- `cicada_user_devices`：`user_id, sn`、`customer_id, create_time desc`、`sn`（设备台账合流）
- `cicada_subscription_logs`：`scene, create_time desc`、`status, create_time desc`（排查订阅推送）

类型规则：`create_time/reset_time=long`、`sort=int`、布尔字段=`bool`、库存数值=`double`、数组字段=`array`、字符串字段=`varchar`。

> 复合索引：在同一面板多次「添加字段」后一起保存。

---

## D. 其余收尾

- [ ] `cicada_orders` schema：CLI 上传时跳过（schema 宽松、不影响写入）。如需同步文档化字段（`customer_id`/`review`），在 HBuilderX 里右键 `cicada_orders.schema.json` → 上传。
- [x] PC 后台正式域名与跨域：`admin.cicadadental.cn` 已完成静态托管部署，并已加入 `env-00jy6g4qwi94` 的跨域白名单。
- [x] PC 后台生产构建配置：`pc-admin/.env.production` 固定指向 `https://env-00jy6g4qwi94.dev-hz.cloudbasefunction.cn`。
- [x] PC 后台版本发布：2026-07-20 已将完整 `pc-admin/dist` 上传到静态托管；线上 `index.html`、JS、CSS 指纹与本地构建一致，真实浏览器加载无控制台错误。
- [ ] 小程序上传：微信开发者工具导入 `unpackage/dist/build/mp-weixin` → 上传 → 微信公众平台提交审核发布。

---

## E. 配完自检

完整测试范围、逐项用例、证据要求和签字模板见 [`上线测试与验收手册.md`](上线测试与验收手册.md)。

### 自动检查（已通过）

```powershell
cd docte-master
npm run check

cd ..\pc-admin
npm run build
npm run check:subscription
npm run check:urls
npm run check:errors
npm run check:security
```

### 真实账号验收（上线前必须完成）

- PC 管理员登录、员工角色权限、退出登录和过期 token。
- 后台工单列表、报价、付款核销、库存、物流、反馈和客户设备。
- 小程序手机号登录、提交报修、报价支付、物流、评价和发票申请。

1. **支付**：小程序提交一单 → 后台发报价 → 客户「确认并支付」走通微信支付 → 工单自动进维修中、`cicada_subscription_logs` 有 `quote_issued` 与 `payment_confirmed` 记录（两条记录使用同一模板 ID，非 skipped）。
2. **身份桥**：用一个**新**手机号小程序下单 → 后台「客户管理」应自动出现该客户（source=miniapp）→ 点进去能看到这张工单。
3. **完成与开票**：把一单推到已完成并确认发票状态 → 仅出现 `order_completed` 推送记录，消息维修结果包含电子发票下载提示；不应出现 `invoice_issued` 推送记录。
4. **设备**：报修带 SN → 「我的设备」出现该设备；后台对应客户的设备台账里也能看到（customer_id 已回填）。

## F. 订阅消息 5 套模板对照

这部分用于和 [`README.md`](README.md) 以及 `.env.example` 保持一致。当前订阅消息只保留 5 套模板，报价并入待支付提醒，开票并入工单完成通知。

| 模板用途 | 环境变量 | 模板 ID |
| --- | --- | --- |
| 报修受理通知 | `WX_SUBSCRIBE_TEMPLATE_REPAIR_SUBMIT` | `16rLpYERsxE1vW8lG9EH2SKAntV2RQQyCuAgOVhuZTk` |
| 设备取货通知 / 回寄发货 | `WX_SUBSCRIBE_TEMPLATE_DEVICE_RECEIVE_SHIP` | `584ePu61H_23o-61K8C_-_rCHnXhlPijAr28zR0_kmk` |
| 待支付提醒 / 维修报价 / 付款成功 / 付款驳回 | `WX_SUBSCRIBE_TEMPLATE_PAYMENT_QUOTE` | `MZ4vej3PNhckxP5JnmjfigQgX14sjuMyN6wk_6Pppsc` |
| 报修进度提醒 | `WX_SUBSCRIBE_TEMPLATE_PROCESS_TIP` | `YbAYEPlqOlYlELUPUEpUHC8PmQ008RiTBK7DUAStQ4` |
| 工单完成通知 / 发票开具 | `WX_SUBSCRIBE_TEMPLATE_ORDER_FINISH_INVOICE` | `TIqcHTeH2T900ANDd5r8L_M6kYMYCtfl9DjkmuXLAFE` |

### 配置说明

- `cicada-client-order` 和 `cicada-admin-order` 都读取同一组 `WX_SUBSCRIBE_TEMPLATE_*` 环境变量。
- `cicada-client-public` 使用同一组变量向小程序返回授权列表，三处映射统一来自 `cicada-subscription-message` 公共模块。
- `process_tip` 作为保留模板继续可用，但不影响当前 5 套主流程。
- `payment_rejected` 复用 `PAYMENT_QUOTE`，`order_completed` 复用 `ORDER_FINISH_INVOICE`。
- `inspection_completed`、`repair_started`、`review_invite`、`invoice_issued` 不再推送。

### 上线前确认项

1. 在 uniCloud 服务空间里配置以上 5 个订阅模板环境变量。
2. 微信公众平台不要调整模板关键词；如调整，必须同步更新公共模块和 [`订阅消息填参规范.md`](订阅消息填参规范.md)。
3. 真机验证一次报修、报价、付款成功、付款驳回、回寄、完工/发票消息链路。
